ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ modules.ScheduleModuleKt.scheduleModule, modules.SchoolClassModuleKt.schoolClassModule, modules.NewsModuleKt.newsModule]
    }
}